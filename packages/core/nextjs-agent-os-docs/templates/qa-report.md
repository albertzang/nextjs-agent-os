# QA report

**Pass:** 1 (pre-merge) | 2 (post-merge) | baseline  
**Backlog work ID:** `{feature-slug}-{NNNN}` | `n/a` (baseline)  
**Environment(s) + exact URLs:**  
**Branch / PR / commit:** (n/a for baseline unless noted)  
**Date:**  
**Result:** pass | pass-with-issues | fail

**Save as:** `{{docs.root}}/reports/QA-pass1.md` (or `QA-pass2.md` / `QA-baseline.md`)

**Retest:** overwrite this same path. Do not create `-prior` / `-v2` / `-attemptN` files.

**Pass 1 scratch (not deliverables):** Ad-hoc local scripts/logs (e.g. `scripts/qa-pass1-*.mjs`, `{{docs.root}}/reports/.qa-pass1-*`) may be used while testing — delete when this report is written; do not commit unless a backlog item adds a maintained harness.

## Scope tested

## Checklist results

| Area | Result | Notes |
|------|--------|-------|
| Public homepage | | |
| Board / Purposes | | |
| Contact | | |
| Admin layout (incl. phone) | | |
| Admin mail | | |
| Admin auth (mail session) | | |
| Admin scaffolds | | |

## Bugs found

List new defects for PM triage (do **not** invent work IDs). Include severity + short repro.  
PM promotes each to a backlog `bug` (**Source:** `qa`).

- (none) | …

Known backlog IDs already under test: link `{{docs.root}}/product/backlogs/{feature}-BACKLOG.md#…` or cite `{feature-slug}-{NNNN}`.

## Suggestions (non-blocking)

## Sign-off

**Pass 1:** merge / hold / retest after fixes  
**Pass 2:** ship confirmed / hotfix needed  
**Baseline:** baseline confirmed / issues found  
